import csv
import stats

let df = csv.read("tests/data/experiment.csv")

print(df)

let filtered =
    df.filter(|row|
        row.age >= 21
    )

let grouped =
    filtered.group_by(
        "condition"
    )

let summary =
    grouped.mean("score")

print(summary)
